import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.joinmatch.app',
  appName: 'JoinMatch',
  webDir: 'dist/football-app/browser',
};

export default config;
